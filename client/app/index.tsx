import { Redirect } from 'expo-router';

export default function Index() {
  // Authentication routing is handled in app/_layout.tsx
  // We just need this file to exist so Expo Router doesn't show the Sitemap
  return null;
}
